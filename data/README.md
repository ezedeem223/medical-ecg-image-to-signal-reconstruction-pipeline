# Data

Place input data here or point the runtime config to an external location.

The maintained runtime expects competition-style assets:

- ECG page images
- `test.csv` or equivalent metadata file
- `sample_submission.parquet` or an equivalent template with `id` rows

This directory is intentionally empty in version control.
