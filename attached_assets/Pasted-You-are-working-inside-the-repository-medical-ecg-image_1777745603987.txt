You are working inside the repository:

medical-ecg-image-to-signal-reconstruction-pipeline

This is the flagship project in my KAUST AI research portfolio.

I want you to build the first safe seed of an ECG Research Workbench.

This must be done on a branch only.

Do NOT modify main.
Do NOT merge to main.
Do NOT fabricate clinical claims, metrics, or validation.
Do NOT use real ECG datasets such as PhysioNet in this first pass.
Do NOT build a web app yet.
Do NOT build a live inference endpoint.
Do NOT create more than 10 synthetic cases in this first pass.

==================================================
BRANCH SAFETY
=============

Before editing, verify that the active repository is exactly:

medical-ecg-image-to-signal-reconstruction-pipeline

Then create or switch to:

git checkout main
git pull
git checkout -b agent-lab/ecg-research-workbench-seed

If the branch already exists, use it safely or create:

agent-lab/ecg-research-workbench-seed-v2

Work only on that branch.

==================================================
PRIMARY GOAL
============

Create a research-oriented ECG Workbench Seed that turns the project from a strong runtime pipeline into a stronger research artifact.

The seed must include:

1. Parametric Synthetic ECG-like Benchmark Seed
2. Synthetic-only Scoring Utility
3. Basic Quality-Control Checks
4. Seed Failure Mode Atlas
5. KAUST Research Brief
6. Lightweight tests and smoke checks

This is not a full research platform yet.
This is the first controlled, reviewable seed.

==================================================
PASS 1 — INSPECT CURRENT REPOSITORY SURFACE
===========================================

Before building, inspect:

* README.md
* configs/models.yaml
* docs/build_notes/source_of_truth.md if present
* docs/build_notes/version_mapping.md if present
* models/README.md if present
* src/ecg_digitizer/runtime/pipeline.py if present
* relevant package entrypoints
* existing tests

Confirm the current model policy before writing docs:

* YOLO lead detection if present
* EffB3 primary segmentation model if verified
* phase-10 / ResNet50 fallback if verified
* calibration / trace extraction / export framing if verified

If any item is not found, mark it as unverified and do not state it as fact.

==================================================
PART 1 — SYNTHETIC ECG-LIKE BENCHMARK SEED
==========================================

Create:

tools/synthetic_benchmark/
├── README.md
├── generate_cases.py
├── render_ecg.py
├── distortions.py
├── metadata.py
└── score_synthetic.py

Build a small synthetic benchmark seed.

Requirements:

* Generate only 5–10 synthetic ECG-like cases by default.
* Use simple parametric ECG-like signals only.
* Do NOT use PhysioNet or external ECG data in this first pass.
* Each case must be clearly labeled synthetic.
* Generate 12-lead-like waveform arrays.
* Render ECG-paper-style images.
* Create metadata JSON.
* Save ground-truth waveform arrays or CSVs.
* Include controlled distortions:

  * clean
  * rotation
  * blur
  * low contrast
  * noise
  * cropped or margin variation if practical

Output directory:

benchmark_cases/seed/
├── README.md
├── synthetic_manifest.json
├── images/
├── waveforms/
└── metadata/

Before committing generated assets, check size.
If generated assets are too large, commit only a tiny curated seed and document regeneration.

Required CLI:

python tools/synthetic_benchmark/generate_cases.py --output benchmark_cases/seed --num-cases 5

==================================================
PART 2 — SYNTHETIC-ONLY SCORING UTILITY
=======================================

Implement:

python tools/synthetic_benchmark/score_synthetic.py 
--ground-truth path/to/ground_truth.csv 
--prediction path/to/prediction.csv 
--output reports/synthetic_score.json

Scoring must be synthetic-only.

Acceptable metrics:

* MAE
* RMSE
* optional SNR-like score if implemented safely

Do NOT claim clinical accuracy.
Do NOT compare to published ECG reconstruction systems.
Do NOT report competition performance.

If prediction format is uncertain, document the expected format and implement a simple array/CSV comparison mode.

==================================================
PART 3 — BASIC QUALITY-CONTROL CHECKS
=====================================

Create:

tools/quality/
├── README.md
├── checks.py
└── generate_quality_report.py

Implement basic QC checks for waveform arrays or CSV-like outputs:

* NaN / Inf detection
* all-zero signal detection
* flatline detection
* signal length consistency
* amplitude range
* variance
* lead count if detectable
* basic warning severity

Required CLI:

python tools/quality/generate_quality_report.py 
--input path/to/waveform.csv 
--output reports/quality_report.md

The QC report must be framed as engineering/research inspection support only, not clinical validation.

==================================================
PART 4 — RESEARCH PACK SEED
===========================

Create or update:

docs/research_pack/
├── README.md
├── PROJECT_BRIEF_KAUST.md
├── FAILURE_MODE_ATLAS.md
├── SYNTHETIC_BENCHMARK_PROTOCOL.md
├── EVALUATION_PROTOCOL.md
└── REPRODUCIBILITY_CHECKLIST.md

PROJECT_BRIEF_KAUST.md must be a concise 2–3 page academic brief.

Include:

* problem
* motivation
* technical pipeline
* current system contribution
* what the workbench seed adds
* limitations
* future KAUST-style research directions

No clinical validation claims.
No diagnostic claims.
No production deployment claims.

FAILURE_MODE_ATLAS.md must include 5–8 failure categories:

* low contrast
* rotation / perspective distortion
* blur
* cropped sheet
* grid weakness
* overlapping leads
* flatline/all-zero extraction
* calibration ambiguity

For each category include:

* failure mode
* likely cause
* expected reconstruction impact
* detection signal
* possible mitigation
* whether synthetic benchmark can simulate it

Label conceptual/synthetic failure modes clearly.
Do not fabricate real failures.

SYNTHETIC_BENCHMARK_PROTOCOL.md must explain:

* why synthetic cases exist
* what they can test
* what they cannot prove
* why they do not replace real ECG evaluation
* how cases are generated
* how scoring should be interpreted

EVALUATION_PROTOCOL.md must include:

* waveform fidelity
* per-lead errors
* alignment sensitivity
* QRS vs low-amplitude segment sensitivity as future direction
* synthetic vs real evaluation distinction
* limitations

REPRODUCIBILITY_CHECKLIST.md must include:

* environment
* configs
* checkpoints
* synthetic benchmark generation
* scoring
* QC reports
* expected outputs
* limitations

==================================================
PART 5 — OPTIONAL FULL-PIPELINE MODE
====================================

Do NOT make full runtime execution mandatory.

If the existing ECG runtime can be invoked safely and all required configs/checkpoints/assets are present, add an optional documented path.

If assets are missing:

* fail gracefully
* document what is missing
* keep generation-only and scoring modes working

The benchmark must be useful even without real ECG checkpoints.

==================================================
PART 6 — TESTS
==============

Add lightweight tests that do not require real ECG data or model weights.

Suggested files:

tests/test_synthetic_benchmark.py
tests/test_synthetic_scoring.py
tests/test_quality_checks.py
tests/test_research_pack_exists.py

Tests should verify:

* synthetic generation creates expected files
* metadata marks synthetic=true
* scoring works on toy arrays
* QC detects NaNs, flatlines, all-zero signals
* research pack files exist

Run:

python -m ruff check .
python -m pytest

If existing project tests fail due to unrelated historical issues, report them exactly and still run targeted new tests if possible.

==================================================
PART 7 — README INTEGRATION
===========================

Update the main README lightly only.

Add a short section:

## ECG Research Workbench Seed

Mention:

* synthetic ECG-like benchmark
* synthetic-only scoring
* quality-control checks
* failure mode atlas
* KAUST research brief

Keep it short.
Do not let this section dominate the existing README.
Do not rewrite the whole README.

==================================================
DO NOT BUILD IN THIS PHASE
==========================

Do not build:

* interactive web app
* Gradio/Streamlit app
* live inference endpoint
* large benchmark dataset
* PhysioNet-based dataset
* real clinical ECG dataset
* DICOM/HL7 integration
* clinical validation framing
* model retraining
* comparison against published baselines
* production medical claims

==================================================
COMMIT / PUSH
=============

After finishing, run:

git status
python -m ruff check .
python -m pytest
python tools/synthetic_benchmark/generate_cases.py --output benchmark_cases/seed --num-cases 5
python tools/quality/generate_quality_report.py --input benchmark_cases/seed/waveforms/case_000.csv --output reports/quality_report.md

If paths differ, use the actual generated first waveform path.

Commit only on the agent-lab branch:

Add ECG research workbench seed

If push is available, push only the agent-lab branch.

Do not merge to main.

==================================================
FINAL REPORT REQUIRED
=====================

Report only:

1. Confirm repository name
2. Confirm active branch name
3. Confirm main was not modified
4. Files/directories created
5. Files updated
6. Synthetic benchmark summary
7. Number of synthetic cases generated
8. Scoring utility summary
9. QC checks implemented
10. Research pack files created
11. README update summary
12. Tests and smoke checks run
13. Whether commit was created
14. Whether branch was pushed
15. What requires real ECG checkpoints/assets
16. What is synthetic-only
17. Exact failures or limitations
