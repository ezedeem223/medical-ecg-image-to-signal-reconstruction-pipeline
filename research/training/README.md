# Research Training

Training survives here only as optional research support.

It is not part of the default maintained runtime path. The repository prioritizes inference/runtime reproducibility first, and keeps only one canonical segmentation-training path here as a secondary research artifact.
